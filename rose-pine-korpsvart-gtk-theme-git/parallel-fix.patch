diff --git a/themes/install.sh b/themes/install.sh
index 2106c758..2ca3dbeb 100755
--- a/themes/install.sh
+++ b/themes/install.sh
@@ -5,6 +5,8 @@ set -Eeo pipefail
 REPO_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" >/dev/null 2>&1 && pwd)"
 SRC_DIR="${REPO_DIR}/src"
 
+mkdir -p "${SRC_DIR}/sass/$$/gnome-shell"
+
 source "${REPO_DIR}/gtkrc.sh"
 
 ROOT_UID=0
@@ -416,41 +418,41 @@ install_package() {
 }
 
 tweaks_temp() {
-	cp -rf "${SRC_DIR}/sass/_tweaks.scss" "${SRC_DIR}/sass/_tweaks-temp.scss"
+	cp -rf "${SRC_DIR}/sass/_tweaks.scss" "${SRC_DIR}/sass/$$/_tweaks-temp.scss"
 }
 
 compact_size() {
-	sed -i "/\$compact:/s/false/true/" "${SRC_DIR}/sass/_tweaks-temp.scss"
+	sed -i "/\$compact:/s/false/true/" "${SRC_DIR}/sass/$$/_tweaks-temp.scss"
 }
 
 moon_color() {
-	sed -i "/\@import/s/color-palette-default/color-palette-moon/" "${SRC_DIR}/sass/_tweaks-temp.scss"
-	sed -i "/\$colorscheme:/s/default/moon/" "${SRC_DIR}/sass/_tweaks-temp.scss"
+	sed -i "/\@import/s/color-palette-default/color-palette-moon/" "${SRC_DIR}/sass/$$/_tweaks-temp.scss"
+	sed -i "/\$colorscheme:/s/default/moon/" "${SRC_DIR}/sass/$$/_tweaks-temp.scss"
 }
 
 blackness_color() {
-	sed -i "/\$blackness:/s/false/true/" "${SRC_DIR}/sass/_tweaks-temp.scss"
+	sed -i "/\$blackness:/s/false/true/" "${SRC_DIR}/sass/$$/_tweaks-temp.scss"
 }
 
 float_panel() {
-	sed -i "/\$float:/s/false/true/" "${SRC_DIR}/sass/_tweaks-temp.scss"
+	sed -i "/\$float:/s/false/true/" "${SRC_DIR}/sass/$$/_tweaks-temp.scss"
 }
 
 outline_style() {
-	sed -i "/\$outline:/s/false/true/" "${SRC_DIR}/sass/_tweaks-temp.scss"
+	sed -i "/\$outline:/s/false/true/" "${SRC_DIR}/sass/$$/_tweaks-temp.scss"
 }
 
 macos_winbutton() {
-	sed -i "/\$window_button:/s/normal/mac/" ${SRC_DIR}/sass/_tweaks-temp.scss
+	sed -i "/\$window_button:/s/normal/mac/" "${SRC_DIR}/sass/$$/_tweaks-temp.scss"
 }
 
 gnome_shell_version() {
-	cp -rf "${SRC_DIR}/sass/gnome-shell/_common.scss" "${SRC_DIR}/sass/gnome-shell/_common-temp.scss"
+	cp -rf "${SRC_DIR}/sass/gnome-shell/_common.scss" "${SRC_DIR}/sass/$$/gnome-shell/_common-temp.scss"
 
-	sed -i "/\widgets/s/40-0/${GS_VERSION}/" "${SRC_DIR}/sass/gnome-shell/_common-temp.scss"
+	sed -i "/\widgets/s/40-0/${GS_VERSION}/" "${SRC_DIR}/sass/$$/gnome-shell/_common-temp.scss"
 
 	if [[ "${GS_VERSION}" == '3-28' ]]; then
-		sed -i "/\extensions/s/40-0/${GS_VERSION}/" "${SRC_DIR}/sass/gnome-shell/_common-temp.scss"
+		sed -i "/\extensions/s/40-0/${GS_VERSION}/" "${SRC_DIR}/sass/$$/gnome-shell/_common-temp.scss"
 	fi
 }
 
@@ -483,7 +485,7 @@ theme_color() {
 			;;
 		esac
 		tweaks_temp
-		sed -i "/\$theme:/s/default/${theme_color}/" "${SRC_DIR}/sass/_tweaks-temp.scss"
+		sed -i "/\$theme:/s/default/${theme_color}/" "${SRC_DIR}/sass/$$/_tweaks-temp.scss"
 	fi
 }
 
@@ -596,6 +598,7 @@ uninstall_theme() {
 	done
 }
 
+
 if [[ "$uninstall" == 'true' ]]; then
 	if [[ "$libadwaita" == 'true' ]]; then
 		echo -e "\nUninstall ${HOME}/.config/gtk-4.0 links ..."
diff --git a/themes/src/sass/_tweaks-temp.scss b/themes/src/sass/_tweaks-temp.scss
index eacba67f..0041cb41 100644
--- a/themes/src/sass/_tweaks-temp.scss
+++ b/themes/src/sass/_tweaks-temp.scss
@@ -1,16 +1,16 @@
 // Theme ColorScheme palette: night/moon/storm
-@import 'color-palette-default';
+@import 'color-palette-moon';
 
-$colorscheme: 'default';
+$colorscheme: 'moon';
 
 // transparent opacity
 $opacity: 'default';
 
 // Theme primary color
-$theme: 'default';
+$theme: 'teal';
 
 // Theme size
-$compact: 'false';
+$compact: 'true';
 
 // Theme translucent
 $translucent: 'false';
@@ -25,7 +25,7 @@ $blackness: 'false';
 $outline: 'false';
 
 // Windows button style : mac/normal
-$window_button: 'mac';
+$window_button: 'normal';
 
 // Gnome-Shell float panel version
 $float: 'false';
